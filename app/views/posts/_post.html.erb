<div id="<%= dom_id post %>">
  <h2><%= post.title %></h2>
  <% if user_signed_in? %>
    <small><%= post.user.name %></small>
  <% else %>
    <%= link_to "Login", new_user_session_path %>
  <% end %>
  <p><%= post.body %></p>
</div>
